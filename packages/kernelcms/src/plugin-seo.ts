/**
 * kernelcms/plugin-seo — first-party SEO plugin.
 *
 *   import { seoPlugin } from 'kernelcms/plugin-seo'
 *   export default defineConfig({ plugins: [seoPlugin({ collections: ['posts'], generateTitleFrom: 'title' })], ... })
 */
export * from '@kernel/plugin-seo'
