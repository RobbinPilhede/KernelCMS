/**
 * kernelcms/graphql — a GraphQL API generated from your content model.
 *
 *   import { createGraphQL, buildGraphQLSchema } from 'kernelcms/graphql'
 *
 * Or enable the built-in endpoint: serve(kernel, { graphql: true }) → POST /api/graphql.
 */
export * from '@kernel/graphql'
