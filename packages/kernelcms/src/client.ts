/**
 * kernelcms/client — a tiny, typed fetch client for the REST API. Browser-,
 * edge-, and Node-safe (uses global fetch; no Node built-ins).
 *
 *   import { createClient } from 'kernelcms/client'
 */
export * from '@kernel/client'
