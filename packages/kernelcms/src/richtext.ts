/**
 * kernelcms/richtext — the editor-agnostic rich-text model and converters.
 *
 * Use the converters on your frontend to render `richText` field content:
 *
 *   import { toReact, toHTML, toPlainText } from 'kernelcms/richtext'
 */
export * from '@kernel/richtext'
