/**
 * kernelcms/server — a web-standard `Request -> Response` REST handler plus a
 * Node http adapter. Server-only (imports node:http).
 *
 *   import { createRequestHandler, serve } from 'kernelcms/server'
 */
export * from '@kernel/server'
