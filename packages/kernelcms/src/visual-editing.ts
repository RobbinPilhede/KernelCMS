/**
 * kernelcms/visual-editing — the framework-agnostic click-to-edit SDK for your
 * own frontend. Mark elements editable and subscribe to live preview data from
 * the KernelCMS admin's preview iframe:
 *
 *   import { kernelEditable, connectVisualEditing } from 'kernelcms/visual-editing'
 *
 * React users import the hook from 'kernelcms/visual-editing/react'.
 */
export * from '@kernel/visual-editing'
