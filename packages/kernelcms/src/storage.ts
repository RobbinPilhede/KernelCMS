/**
 * kernelcms/storage — swappable byte storage adapters for upload collections.
 *
 *   import { localStorage } from 'kernelcms/storage'
 *   export default defineConfig({ storage: localStorage({ rootDir: './.uploads' }), ... })
 */
export * from '@kernel/storage'
