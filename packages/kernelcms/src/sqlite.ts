/**
 * kernelcms/sqlite — the zero-dependency SQLite adapter (Node's built-in
 * `node:sqlite`). Requires Node >= 22.
 *
 *   import { sqliteAdapter } from 'kernelcms/sqlite'
 */
export * from '@kernel/db-sqlite'
